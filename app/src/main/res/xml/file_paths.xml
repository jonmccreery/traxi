<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Only the export staging directory is shared. Raw .bin dumps live in
      internal storage and are never exposed through the provider: sharing is
      an explicit act on a derived file, not blanket access to the flash
      images.
    -->
    <cache-path name="exports" path="exports/" />
</paths>
